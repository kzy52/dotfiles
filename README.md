dotfiles
===================

Install
-------

Clone from github

```
$ git clone --recursive git@github.com:kzy52/dotfiles.git
```

install packages.

```
$ brew install zsh zsh-completions tmux ag reattach-to-user-namespace tig jq peco awscli
```
